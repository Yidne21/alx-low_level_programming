#include "main.h"
/**
 * mul - multiply tow integer numbers
 * @a: is an integer to be multiplied
 * @b: is an integer to be multiplied
 *
 * Return: the product on success
 */
int mul(int a, int b)
{
	return (a * b);
}
