/**
 * _isupper - identify uppercase character
 * @c: is an integer ASCII value of the character 
 *
 * it doesn't have return value it is just a header file
 */
int _isupper(int c);
int _isdigit(int c);
int mul(int a, int b);
